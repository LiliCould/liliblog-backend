# 数据源配置
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/liliblog?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
  # 编码处理
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
  mail:
    host: smtp.qq.com
    port: 587
    username: lilicould@qq.com
    password: xasakbeliasbcibh
    default-encoding: UTF-8
    protocol: smtp
    properties:
        mail:
          smtp:
            auth: true
            starttls:
              enable: true
              required: true
  data:
    redis:
      database: 4
      host: localhost
      port: 6379
      password: 123456
      timeout: 5000
      lettuce:
        pool:
          max-idle: 16
          min-idle: 8
          max-active: 32

# MyBatis配置
mybatis:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath:mapper/*.xml
  
# JWT配置
jwt:
  secret: mynameisliliandiamabackenddeveloper
  # 过期时间七天
  expire: 604800000

server:
  base-url: http://localhost:8080

# 文件上传路径
file:
  upload:
    path: ./uploads/